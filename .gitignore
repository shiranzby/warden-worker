target
node_modules
.wrangler
.env
# Frontend files (downloaded during deployment/development)
public/web-vault/

# reference repos
vaultwarden/
bw_web_builds/
# 稀疏检出的前端源码(写 webvault/patches 用), 由 webvault/sync-source.sh 生成
.vwsrc/

# regression test screenshots (generated by tests/mobile-regression.mjs --shot)
tests/shots/

# Generated seed SQL (global equivalent domains)
sql/global_domains_seed.sql

# 部署后验证的临时目录(轮询 CI / 抓取线上产物), 由 skill 的 verify-deploy.sh 使用
.ci/
#
# .deploycheck/ —— 本地验证目录。里面既有**要入库的脚本**(回归闸门), 也有 GB 级的
# 产物快照/离线包。所以按"**显式白名单**"处理:
#   · 先忽略目录下所有内容(`.deploycheck/*`, 注意不能写成 `.deploycheck/` ——
#     整个目录被忽略时 git 不会进入它, 后面的 `!` 白名单会被一并忽略掉);
#   · 再逐条放行脚本。
# 🔴 **绝不能用"按扩展名放行"**(如 `!.deploycheck/*.mjs`) —— 该目录里还有 18 个
#    早期脚本带着**测试账号 + 主密码**明文(9 个直接硬编码, 9 个写成 `process.env.X || "明文"`
#    的兜底), 按扩展名放行会把它们提交到 public 仓库。新增要入库的脚本时, 必须
#    **逐个加白名单**, 加之前先在 .deploycheck/ 里跑扫描(完整模式与理由见 .deploycheck/README.md):
#      PAT='(ACCOUNT|MAIL|EMAIL|PASSWORD|PASS|TOKEN|SECRET)[A-Za-z_]*[[:space:]]*[:=][^"]{0,60}"[A-Za-z0-9._@!-]{8,}|ghp_[A-Za-z0-9]{20}|github_pat_[A-Za-z0-9_]{20}'
#      grep -lE -- "$PAT" * 2>/dev/null
#    ⚠️ 别把真实的账号前缀/密码前缀写进本文件 —— 它是**公开**的; 也别把裸 `PAT` 加回变量名
#       列表(`PATCHED`/`PATH` 会误伤)。凭据统一放 `.deploycheck/.env.local`(被下面第一条忽略),
#       脚本从环境变量读。
.deploycheck/*
!.deploycheck/README.md
!.deploycheck/b8-lib.mjs
!.deploycheck/verify-batch10.mjs
!.deploycheck/verify-batch11.mjs
!.deploycheck/verify-batch12.mjs
!.deploycheck/verify-batch13.mjs
!.deploycheck/verify-batch14.mjs
!.deploycheck/verify-batch15.mjs
!.deploycheck/verify-j15.mjs
!.deploycheck/verify-p7-browser.mjs
# 第十七批/U 段的三道回归闸门(窄屏验收 + 桌面回归 + 线上验收)。入库前已跑过上面的凭据扫描, 无命中。
!.deploycheck/probe-u17-verify.mjs
!.deploycheck/probe-u17-desktop.mjs
!.deploycheck/probe-u17-live.mjs
!.deploycheck/devserver-b8.config.js
!.deploycheck/poll-progress.sh
!.deploycheck/run-artifact-asserts.py
# CI 守卫审计: 查 build-web-vault.yaml 里 '^' 顶格守卫有没有**哑弹**(永远不命中)。
# 第十四批实测踩到过 —— 守卫模式漏抄一个词, 把规则写成全局它也不报警。
!.deploycheck/probe-audit-guards.py
!.deploycheck/probe-s15-guard.py
# 第十六批/T 段的验收闸门(6 个)。入库前已按上面的 PAT 扫过, 全部只从 .env.local 读凭据。
# 每个脚本头部都写了"为什么这么断 / 实测数据", 别只留结论。
# ⚠️ 下面每一条**必须独占一行**: .gitignore **不支持行尾注释** —— 写成
#    `!.deploycheck/x.mjs   # 说明` 会把 `# 说明` 一起当成模式, 于是这条白名单静默失效
#    (文件仍被忽略, 而 `git status` 里看不出来)。注释只能单独占一行。
!.deploycheck/probe-b16-totp-frames.mjs
# ↑ 放慢 10 倍逐帧截图, 拍到"升格前 4 帧还是旧码"的那个
!.deploycheck/probe-b16-totp-roll.mjs
# ↑ T3 验收: f0 主码须已是新码, 且全程 ghost !== digits
!.deploycheck/probe-b16-select-postfix.mjs
# ↑ T1 验收: 窄屏 开/再点收起/600ms 无关闭帧; 宽屏 4 项桌面行为不变
!.deploycheck/probe-b16-domain.mjs
# ↑ T2 验收: 按钮中心 vs 输入框中心的几何差(9 元组全量打印)
!.deploycheck/probe-b16-notes.mjs
# ↑ S2 收窄的回归守卫: 备注框不许被压成 32px
!.deploycheck/falsify-b16.py
# ↑ 守卫**离线证伪**(把改动改回去必须报错), 防哑弹
!.deploycheck/probe-b16-select-webkit.mjs
# ↑ 第二引擎(WebKit≈iOS Safari)复验 T1: Chromium 复现不出的触摸路径, 用 WebKit 兜底
!.deploycheck/probe-b16-live.mjs
# ↑ 线上(shypwd.cc.cd)最终验收: T3 升格顺序 + T1 展开 + T2 几何, 走 7890 代理
!.deploycheck/shot-b16-live.mjs
# ↑ 线上截图取证(只拍设置页, 不含保险库条目内容); 图落 shots/ 不入库
!.deploycheck/probe-v18-verify.mjs
# ↑ 第十八批(V 段) 窄屏验收(390×844, dev server 8099): 五题逐条断, 含负向回归
!.deploycheck/probe-v18-live.mjs
# ↑ 线上(shypwd.cc.cd)最终验收: V 段五题 + 应用内深色主题, 走 7890 代理
!.deploycheck/probe-w19-verify.mjs
# ↑ 第十九批(W 段) 验收: 同一脚本跑两个环境 —— 不带 WARDEN_TEST_PROXY 走 8099 dev server,
#   带上则走线上(本机直连不通, 必须代理)。判据量的是"用户看见的框", 不是控件本体。
!.deploycheck/probe-x20-verify.mjs
# ↑ 第二十批(X 段) 验收: 用 RECON_W/RECON_H 一个脚本跑**两个视口宽度**(默认桌面 1280)。
#   本批的教训就是"只跑 390px ⇒ 电脑端功能性缺陷活了 6 批", 所以默认值刻意是桌面。

# Coding assistant
.ace-tool/
.codex/
.cursor/
.claude/